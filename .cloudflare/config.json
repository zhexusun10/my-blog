{
  "name": "my-blog",
  "type": "pages",
  "build": {
    "command": "npm run build",
    "output_directory": ".next",
    "root_directory": "."
  },
  "routes": [
    { "pattern": "/*", "script": null }
  ]
} 