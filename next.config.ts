import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['source.unsplash.com', 'images.unsplash.com', 'i0.wp.com', 'via.placeholder.com'],
  },
};

export default nextConfig;
